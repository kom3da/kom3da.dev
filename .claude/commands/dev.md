Start the local development server with Wrangler and verify it's running.

Steps:
1. Kill any existing process on port 8787 if occupied
2. Run `npm run dev` in background
3. Wait for the server to be ready, then confirm the URL
4. Test the root page responds with 200
